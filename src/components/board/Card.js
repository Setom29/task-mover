import React from 'react'
import {Typography, Box} from "@mui/material"

export default function Card(cardId, users, setUsers, cards, setCards, comments, setComments) {
  return (
    <Box>
        <div>Card</div>
        <Typography/>
    </Box>
  )
}
